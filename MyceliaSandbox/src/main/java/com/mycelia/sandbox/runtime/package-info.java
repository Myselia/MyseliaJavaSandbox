/**
 * <strong>The Mycelia Runtime.</strong> <br />
 * 
 * All the classes that makes up the runtime.
 * Those classes executes a Mycelia application.
 * There is multiple types of runtime that can be used depending
 * on where and how you want to run the Mycelia application.
 * A typical run would use a runtime of type Network.
 * If you just want to test your application on your local computer, use the runtime type Local.
 */
package com.mycelia.sandbox.runtime;